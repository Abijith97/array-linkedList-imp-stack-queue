name: PR Pipeline

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: write
  contents: read

jobs:
  tests:
    uses: ./.github/workflows/tests.yml

  code-review:
    needs: tests
    uses: ./.github/workflows/auto-code-review.yml
    secrets: inherit